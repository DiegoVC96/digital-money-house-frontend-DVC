import "./globals.css";

export const metadata = { title: "Digital Money House", description: "Billetera virtual Digital Money House" };

export default function RootLayout({ children }) {
  return <html lang="es"><body>{children}</body></html>;
}
